export type user = {
   id: string
   name: string
   email: string
   age: number
}

export type product = {
   name: string
   description: string
   price: string
}

export type ticket = {
   id: string
   name: string
   description: string
   price: string
   leaving: string;
   going: string;
}

export type purchase = {
   id: string
   name: string
   description: string
   price: string
   quantity: number
}