### Exercício 1

    a) A resposta vem igual quando utilizamos o Mysql, um array grandão
    
    b)  const result = await connection.raw(`
        SELECT * FROM Actor where name = ${};
        `);

    c)  const result = await connection.raw(`
        SELECT COUNT(*) as count FROM Actor WHERE gender = "${gender}"
        `);

    